# Frontend Review Notes — hw-06

Дата: 2026-08-30

Огляд виконаний за промптом `frontend-review.md` — перевірено `index.html` і `style.css`.

Проблеми та рекомендації (використовувати формат з `frontend-review.md`):

🔴 Critical

- Дубльований `id="email"` у формі (див. `index.html`).

🟠 Important

- Невірна структура таблиці (`section#table-ntf`).
- Некоректні стилі фокусу (`input:focus`).

🟡 Improvement

- Alt-текст логотипу змішаний мовами.
- Глобальний селектор `li:first-child` може впливати на інші списки.

🟢 Good

- Присутній `lang="uk"` та `meta viewport`.

---

*(Детальний огляд збережено згідно з промптом.)*